# Grilling 记录

## 已接受的方向

- 本任务文档统一放在 docs/fix-prolific-return-and-aw-no-submit/。
- 接入 submission.status.change 核查平台状态。
- RETURNED 的结果退出正式计数并归档；原有归档位置已获用户同意。
- 对 AW 无本地结果核查后发送退回请求；不能仅凭完成码判定结果缺失。

## Round 1 — 已接受（用户回复“可以”，2026-09-09）

1. AW 检查范围：仅异常码，还是所有 AWAITING REVIEW？建议所有待审核，完成码只辅助分类；正常码缺结果也必须捕捉。
2. 自动消息触发：仅一次文件不存在即可发送，还是延迟复查且排除草稿/身份错配/故障后才发送？建议先等待 10 分钟再核查（提案值），疑点转人工，发出前再次检查结果与平台状态；每 session 最多一次。草稿或其他 session 有结果不能直接算作本次已完成，但需人工核查。
3. RETURNED 同步范围：同时释放未提交占位并阻止该旧 session 重新计数，还是只归档结果文件？建议前者。TIMED-OUT 是否纳入同一版另待确认，不自动扩大范围。
4. RETURNED 与撤回同意：普通退回按既定方案归档；明确撤回同意另行处理而非永久归档，是否接受？建议区分，不从状态推断动机；具体删除范围/保留策略待该分支确认后细化。

## 后续依赖问题（尚未决定）

- 自动消息的准确文案、补做与退回的关系、首次启用是否发送历史缺失记录。
- 状态随后变为 APPROVED 的归档处理；不能无条件恢复导致超额。
- API 不可用、重复/乱序事件、身份不匹配与并发提交的处理。
- 部署方式、API 凭证/工作区、HTTPS 接入、签名验证与补偿对账。
- TIMED-OUT、错误提示隐藏修复是否属于本版。
- 先只读观察再开启写入/消息的上线验收条件。

确认设计与文案后才启用生产自动消息或迁移。此要求来自用户此前的线上变更约束，技能也要求达成共同理解后再执行方案。创建本目录已获用户明确授权。

## Round 1 决议摘要

用户接受四项建议：检查全部 AWAITING REVIEW；缺失等待 10 分钟再复查，有草稿/身份错配/其他 session 结果/保存故障疑点转人工，发送前再核查且每 session 最多一次；RETURNED 同时归档结果、释放未提交占位并阻止旧 session 重入；普通退回与明确撤回同意分开处理。

## Round 2 — 已接受（用户回复“可以”，随后追问历史数据来源）

1. 自动消息文案：建议明确说明该 session 未找到最终标注、请求 return；若认为已提交可回复核查。不指责参与者，不承诺支付或自动补做。首次发送需以确认后的英文文案为准。
2. 历史记录：建议上线前跑一次全量只读对账并列待发清单；历史记录经确认后发送，新事件按已接受条件自动处理。此前收到过退回请求的记录不重复发送。历史状态必须来自最新 API，不复用旧 CSV 直接发送。
3. 本版范围：建议同时纳入 TIMED-OUT 未提交占位释放与错误提示隐藏修复。TIMED-OUT 若有最终答案，保留并转人工，不自动按 RETURNED 归档。
4. 已归档记录后来变为 APPROVED 等状态：建议报警转人工，不自动恢复正式计数，以免已补招后超过每sample目标。明确撤回同意的删除请求同样转人工处理，不只永久归档。

待 Round 2 确认后，进一步确定凭证/HTTPS接入、补偿对账频率、服务故障和并发的技术策略，以及上线验收和开关。尚未发送消息或部署。

## 历史处理澄清与消息 API 核查

用户已接受 Round 2 的文案、历史清单先确认、新事件自动处理、TIMED-OUT 未提交占位释放、错误提示修复和状态反转转人工。未授权立即发送生产消息。

Q2 通过当前平台状态全量对账处理遗留差异，不依赖或假定 webhook 历史回放。当前应用没有自己保存的 webhook 历史。用 return_requested 和可访问的聊天记录排除已有请求；消息历史不可访问或不能明确关联 session 时转人工。

官方消息 API 可以读取和发送聊天。按研究批量获取需 created_after + study_id；时间查询只允许最近 30 天。按 user_id 查询的更早历史完整性不能从当前文档保证，必须实测。工作区消息查询需要 workspace messaging 已启用及成员权限。聊天按参与者/研究组织，不能假定天然一一对应 session；重复参与时尤其需避免误匹配。

## 消息历史策略 — 已接受

用户回复“OK，没问题”：同意使用消息 API 与 return_requested 辅助去重；按 session 关联，不把同一参与者其他 session 的请求混用；无法明确关联或权限不足时转人工。该回复未授权立即发送任何生产消息。

## Round 3 — 已接受（用户回复“同意”）

1. 上线顺序：建议先完成隔离测试，接入真实 API 后仅只读生成对账与拟执行动作清单；用户确认清单及验证结果后再启用归档/占位释放/自动消息。历史消息仍单独按已接受清单确认规则执行。
2. 外部故障策略：API 或聊天查询失败时不把失败当成“无结果”，不迁移或发送；保留待处理事件并重试。API 发送超时但可能已成功时标记结果不确定，先核对消息/return_requested，无法确认则人工处理，不能直接重发。

技术前提待查：实际 API 凭证权限、workspace、现有 webhook 订阅和签名 secret、HTTPS 回调部署位置。不得覆盖既有 workspace secret 或其他订阅。未读取凭证、未注册 webhook、未发送消息。

## to-spec 合成

业务访谈已结束；正式规格已合成。测试边界按技能要求仍需一次核对。tracker/triage项目配置未发现，发布待 /setup-matt-pocock-skills。未部署或发送消息。

## to-tickets 审核结果

用户已批准 8 个垂直切片及其阻塞关系。各票独立稿件位于任务 docs 的 issues 子目录；外部或本地 tracker 发布等待配置。未发布 issue、未修改父 issue、未实施生产功能。
