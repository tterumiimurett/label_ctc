# Tickets — vertical-slice proposal

状态：用户已批准 8 张票的粒度与阻塞关系；独立票稿已生成，等待 tracker 配置后发布。
来源：[已合成规格](spec.md)。原按模块划分的实施清单已由以下端到端切片替代。

用户要求本任务文档保留在当前 docs 目录。若选择本地 tracker，每票一个文件也应保留在任务目录下，优先遵循用户的目录要求；tracker 类型尚未配置，不擅自选择 GitHub 或本地发布。

## 01 — 参与者能看到加载失败原因

**Blocked by:** None (can start immediately).

**What to build:** 从实际分配失败响应到页面可见错误提示打通错误路径，保留成功加载路径。

- [ ] 任务不足、网络/JSON错误均有可见提示，不只显示标题和身份。
- [ ] 成功路径在真实浏览器展示任务、说明并可播放音频。
- [ ] 使用隔离 session 和数据验证，不为测试触发真实参与者分配。
- [ ] 展示可验证的改动；生产上线仍受整体启用流程约束。

## 02 — 研究者能只读查看平台与本地差异

**Blocked by:** None (can start immediately).

**What to build:** 从平台当前 submission 状态到本地答案核对，输出分类与拟执行动作的只读报告，建立后续动作共用的对账入口。

- [ ] 核对研究、session、参与者；分别展示平台计数、本地答案、临时占位。
- [ ] 全量 AW 按完成码和结果有无分类，检查正式/归档/草稿/其他session结果。
- [ ] 展示 RETURNED 与 TIMED-OUT 的本地关联情况，不修改业务数据或发送消息。
- [ ] API/文件读取失败是未知或故障，不被算成无答案；遍历完整结果并遵循API分页/限流约束。
- [ ] 复用现有分配与提交测试模型，使用临时存储和可控平台响应；必要的轻量接口提取在本票内完成，不做独立宽泛重构。

## 03 — 已退回 session 归档并释放名额

**Blocked by:** 02 — 研究者能只读查看平台与本地差异。

**What to build:** 对账确认 RETURNED 后，受控执行归档/释放，使下一个合格新 session 可领取正确名额。

- [ ] 有最终答案则保留内容和审计证据归档；无最终答案则仅释放占位。
- [ ] 与正常提交/分配并发和进程中断时可恢复，不丢数据、不重复释放、不超额。
- [ ] 旧 RETURNED session 不能重新占位或使归档结果再次计数。
- [ ] 已归档后状态反转转人工，不自动恢复；明确撤回同意单独标记处理。
- [ ] 隔离演示“退回→归档/释放→新分配”，默认生产执行关闭。

## 04 — 平台超时后正确释放未提交占位

**Blocked by:** 03 — 已退回 session 归档并释放名额。

**What to build:** 复用经验证的占位释放和旧session防重入机制，让 TIMED-OUT 且无答案的任务可以重新分配。

- [ ] 无答案则释放；有最终答案则保留并转人工，不套用RETURNED归档策略。
- [ ] 重复处理及超时后的迟交不重复计数或超额。
- [ ] 隔离演示超时到重新分配，验证有答案分支不被迁移。

## 05 — 确认缺失后生成唯一待联系请求

**Blocked by:** 02 — 研究者能只读查看平台与本地差异。

**What to build:** 从 AW 缺失发现经过十分钟复查、消息历史核查，到生成带已确认文案的待发请求或人工核查项；不实际发送。

- [ ] 检查全部AW；完整答案不因NOCODE/未知码触发请求。
- [ ] 十分钟等待可跨重启，重复事件不跳过等待或生成重复请求。
- [ ] 草稿、身份错配、其他session结果、保存故障、历史访问失败或关联不明转人工。
- [ ] 核对 return_requested 与可访问聊天，按session去重；不承诺不存在的消息历史覆盖。
- [ ] 报告展示确切 session、证据及已确认文案，历史候选清单可单独确认。

## 06 — 安全发送退回请求并记录结果

**Blocked by:** 05 — 确认缺失后生成唯一待联系请求。

**What to build:** 对获准候选发送一条退回请求，并可判断已发送、取消、失败或结果不确定。

- [ ] 发送前再次检查当前平台状态和最终答案，已到达答案或状态变化取消发送。
- [ ] 采用一种消息操作保留已确认文案，不同时发结构化请求和普通消息。
- [ ] 持久化发送状态，每session最多一次；发送超时先查证，不盲目重试。
- [ ] 不确定且无法查证转人工；发送成功不视为RETURNED，不改变审核/支付或立即释放名额。
- [ ] 隔离完整验证消息副作用；真实发送默认关闭，历史清单须批准。

## 07 — 状态通知自动触发安全对账

**Blocked by:** 02 — 研究者能只读查看平台与本地差异。

**What to build:** 从公网HTTPS签名事件到可靠持久化、当前API状态核查和只读对账报告，实现自动触发；定期当前状态对账补偿漏事件。

- [ ] 验签、研究隔离、事件去重、乱序处理与中断恢复均可验证。
- [ ] 通知只作为核查触发源，不假定包含完成码，也不假定接入前历史事件可回放。
- [ ] API故障保留待办并重试，不触发迁移或消息；调度参数明确可配置。
- [ ] 核实账号/workspace权限及HTTPS条件，保留其他订阅/secret，不覆盖既有集成。
- [ ] 完整演示“签名事件/定时触发→最新状态→对账报告”，本票不启用生产动作。

## 08 — 只读验收后启用完整同步流程

**Blocked by:** 01、04、06、07（03、05、02 已由这些票传递阻塞）。

**What to build:** 将自动触发接入已验证的归档、超时释放和消息动作，先在真实API只读运行，再按获批清单和开关启用。

- [ ] 使用当前平台状态输出完整只读动作清单，验证权限和关联准确性。
- [ ] 隔离验证完整链路：状态事件→对账→归档/释放或消息；迟交、重启、乱序不破坏约束。
- [ ] 用户确认报告和验证结果后才启用生产迁移/释放/消息；历史消息清单单独确认。
- [ ] 独立记录每个动作和失败原因，可关闭后续自动动作；不把回滚当作自动恢复已归档结果。
- [ ] 提供真实浏览器任务、说明、音频与失败提示验证证据，缺失证据明确列出。

## Tracker publication

未发现明确 tracker/triage 配置，仅有 GitHub remote。按 to-tickets 要求运行 /setup-matt-pocock-skills 配置后再发布；拆分已获用户批准。发布时每票独立、依赖先行，使用 ready-for-agent 标签及 tracker 支持的原生 blocking 关系。不得修改/关闭父 issue。

## Approved individual ticket drafts

用户已回复“可以”，批准上述拆分及阻塞关系。以下文件为发布前的独立票稿；不是已配置本地 tracker 的声明，也不是生产上线授权。

- [01: 参与者能看到加载失败原因](issues/01-visible-loading-errors.md)
- [02: 研究者能只读查看平台与本地差异](issues/02-read-only-reconciliation.md)
- [03: 已退回 session 归档并释放名额](issues/03-returned-archive-and-release.md)
- [04: 平台超时后正确释放未提交占位](issues/04-timed-out-claims.md)
- [05: 确认缺失后生成唯一待联系请求](issues/05-missing-result-contact-candidates.md)
- [06: 安全发送退回请求并记录结果](issues/06-send-return-requests.md)
- [07: 状态通知自动触发安全对账](issues/07-webhook-and-periodic-reconciliation.md)
- [08: 只读验收后启用完整同步流程](issues/08-validate-and-activate.md)

## Implementation review — 2026-09-09

Tickets 01 and 02: changes committed in isolated branches, but **changes requested / not accepted**. See [two-axis review](reviews/tickets-01-02-review.md). No deployment or merge.

## Approved execution order and review gate

用户明确授权流程：每张票使用 Implement；完成后执行 Code Review 双轴审查；发现问题直接交回原Session修复并复审；验收通过后才合入主代码库。整轮全部合入后才能进入下一轮。

- Round A: 01、02 修复 → 复审 → 合入。
- Round B: 03、05、07 并行 → 各自审查修复 → 合入。
- Round C: 04、06 并行 → 各自审查修复 → 合入。
- Round D: 08 → 审查修复 → 合入。

05 必须为可交互独立 Session，其他票可非交互独立 Session；不是用子代理替代独立实现会话。审查技能要求的双轴审查代理可单独使用。需要用户决策/人工核查的事项交给用户；业务功能的生产消息/迁移启用仍按既定只读清单确认流程，不与代码合入混同。

2026-09-09 复审：01 已改正确应用，但需音频真实播放推进验证；02 尚需真实API分页契约、全局读取故障传播、完成码未知配置修复。第二轮修复已下发原Session，尚未合入。
