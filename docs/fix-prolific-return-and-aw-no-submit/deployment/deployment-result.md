# 标注服务切换结果

用户确认备份→原参数重启→验证入口；同步及历史动作不开启。

已完成一致备份并重跑原脚本。首次新进程未持续存活，浏览器发现502；随后使用独立会话启动，PID1899923，跨命令验证存活且公网入口恢复200。未将第一次健康200视为部署成功。

原100候选、每任务3人、bundle1、本地240分钟及原数据目录沿用。最终386原数据文件hash与一致备份完全相同，未新增参与者分配/提交。备份位于logs/deployment-backups/20260910T093911Z，为私有未提交运行产物。完整验证摘要restart-validation.json。

演示页为独立静态副本，只含合成音频和虚构身份，任务分配/草稿请求在页面本地响应，提交禁用。真实浏览器确认任务/说明可见、音频实际播放、无页面错误、零生产API请求。生产/verify仅验证入口HTML，不宣称真实参与者分配已端到端验收。未修改或绕过浏览器安全设置，现有HTTP入口保持原样。

公开真实研究样例的初始演示方案被自动审批拒绝，未执行；改为完全合成数据后成功。同步器、HTTPS webhook、历史归档和真实消息均未启动。

## 后续用户授权的真实样例演示

用户随后明确要求用真实研究样例操作，已将隔离演示页切换为一个真实音频任务，身份仍为虚构DEMO、所有生产API请求被本地演示响应替代、提交禁用。真实浏览器验证16.655979秒音频加载播放，选项可选择、时间编辑和复选项在音频重载后保留；零生产API请求。原386文件hash仍完全相同。匿名证据real-sample-preview-validation.json，真实任务内容及截图未提交Git。本项不证明真实参与者提交保存链路，仅证明真实样例在部署界面的加载及交互。

## 用户确认

用户查看真实研究样例隔离演示后明确回复“可以的”，该页面加载、音频与标注交互核验通过。标注服务切换与演示验收阶段完成。该批准不扩展为自动同步、历史归档、真实消息或实际参与者提交写入的授权；这些仍按既定部署步骤处理。无需再次询问本演示页面是否通过。
