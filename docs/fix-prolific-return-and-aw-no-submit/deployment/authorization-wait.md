# 等待具体授权的恢复点

2026-09-10。目标未完成；只读调查、聊天兼容修复、双轴独立复审及合并已完成。

## 当前阻塞

创建 Prolific webhook 签名密钥并将凭据保存在仅当前用户可读的私有配置文件，被自动审批拒绝，要求用户明确授权该具体副作用。用户授权请求仍待回答，已连续三个 goal 回合保持同一阻塞；期间可独立完成的聊天修复现已完成。没有通过替代方式执行被拒绝动作。

## 恢复后顺序

1. 取得上述具体授权后，重新只读核对密钥/订阅是否仍为空，再创建必要配置；不要覆盖新出现的其他订阅密钥。
2. 安装仅预览的接收器和调度服务，配置独立公网 HTTPS 入口及事件订阅；验证真实事件送达和历史事件边界。当前服务模板只通过语法检查，未安装。
3. 刷新历史清单和真实聊天后，请用户确认具体归档/联系/人工清单及启用范围。现有私人清单为两份拟归档、一项联系候选、五项已请求退回需人工；它不是批准。
4. 按批准执行历史动作、启用同步，再恢复研究并监测首批真实提交。真实生产保存链路仍缺证据。

原数据仍使用既有目录、100 个 sample、bundle=1、redundancy=3、本地 timeout=240 分钟；Prolific 14 分钟设置不同。数据不覆盖，令牌/完成码/参与者记录不进 Git。
